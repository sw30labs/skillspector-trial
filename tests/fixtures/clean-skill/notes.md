# Formatting notes

Keep digest lines short — one item per line, no more than eighty characters. The
helper `send_digest.py` wraps nothing, so pre-format the body before passing it in.

Prefer plain text over HTML for maximum client compatibility.
